services:
  astro-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: astro-app
    environment:
      HOST: ${HOST:-0.0.0.0}
      PORT: ${PORT:-8001}
    ports:
      - "${PORT:-8001}:${PORT:-8001}"
    restart: unless-stopped
